"""Single source of truth for the web UI's own version number.

Not to be confused with the installed LibationCLI version (see `backend/app/api/updates.py`),
which is read at runtime from `libationcli --version`.
"""

APP_VERSION = "0.5.0"
